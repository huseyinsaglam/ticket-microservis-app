spring:
  cloud:
    config:
      uri: http://localhost:8888
      profile: native
    stream:
      bindings:
        output:
          destination: msqueue
  application:
    name: notification-service
